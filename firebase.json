{
  "database": {
    "rules": "rules/database/database.rules.json"
  },
  "firestore": {
    "rules": "rules/firestore/firestore.rules"
  },
  "functions": {
    "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run lint"]
  },
  "storage": {
    "rules": "rules/storage/storage.rules"
  }
}
